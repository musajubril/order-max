extends layout
block content
  #content
    nav.navbar.navbar-expand.navbar-light.bg-white.topbar.mb-4.static-top.shadow

      ul.navbar-nav.ml-auto
        .topbar-divider.d-none.d-sm-block
        //  Nav Item - User Information 
        li.nav-item.dropdown.no-arrow
          a.nav-link.dropdown-toggle#userDropdown(href='#' role='button' data-toggle='dropdown' aria-haspopup='true' aria-expanded='false')
            span.mr-2.d-none.d-lg-inline.text-gray-600.small 
        .dropdown-divider
        li.nav-item.dropdown.no-arrow
          a.nav-link.dropdown-toggle#userDropdown(href='/users/logout' role='button' data-toggle='dropdown' aria-haspopup='true' aria-expanded='false')
            span.mr-2.d-none.d-lg-inline.text-gray-600.small
            i.fas.fa-sign-out-alt.fa-sm.fa-fw.mr-2.text-gray-400
            | Logout

    .container-fluid
      .card.shadow.mb-4
        .card-body
          .table-responsive
            .row
              .col-sm-12.col-md-6
              .col-sm-12.col-md-6
                label
                  | Search:
                  input#myInput.form-control.form-control-sm(type='search' )
            .row
              .col-sm-12
                table.table.table-borderless(width='100%' cellspacing='0' style='width: 100%;')
                  thead
                    tr
                      th Name
                      th Email
                      th Particulars
                      th No. of Pairs
                      th Size
                      th Brand
                  each order in orders
                    tbody#myTable
                      tr
                        td 
                          a.btn(href=`/admin/${order._id}`)= order.name
                        td= order.email
                        td #{order.particular} #{order.sole} sole
                        td= order.pairs
                        td= order.size
                        td= order.brand